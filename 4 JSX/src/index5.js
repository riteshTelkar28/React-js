import React from "react";
import ReactDom from 'react-dom';
import {createRoot} from 'react-dom/client';
import myResult from "./test";

createRoot(document.getElementById("root")).render(myResult);
