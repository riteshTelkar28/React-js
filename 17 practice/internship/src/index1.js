import React, { useEffect } from "react";
import { createRoot } from "react-dom/client";
import DataComponent from "./DataComponent";

createRoot(document.getElementById("root")).render(<DataComponent/>);